Unified Public & Private Markets Platform + Data Migration Experience
🚀 Build a Replit Application With the Following Requirements

You are to build a single Replit application that contains two fully functional demo modules presented in a clean, modern UI:

Use Case 1: Unified Public & Private Markets Platform

Use Case 2: Modern Financial Data Platform Migration

Use a clean professional look, suitable for a CIO demonstration, using a design system similar to Material UI / Chakra UI / Tailwind (agent chooses one).

Use a React front-end and a simple backend (FastAPI or Node/Express) that returns static JSON for mock data.

================================
📌 GLOBAL REQUIREMENTS
================================
General

Create a single-page app with routing:

/unified-portfolio

/data-migration

Include a top navigation bar with:

App name: “Unified Markets Demo Platform”

Navigation links to the two use cases.

CIO-grade polish (subtle gradients, sharp typography).

Ensure the design looks clean on desktop.

Libraries to Use

React + Vite (preferred)

Chart.js or Recharts (for donut, treemap, heatmap)

Framer Motion (for smooth animations)

Tailwind or Material UI (your choice)

Mock backend:

Use FastAPI or Node to expose endpoints returning JSON.

===========================================
💰 USE CASE 1 – Unified Public & Private Markets
===========================================
🎯 Goal

Show a holistic cross-asset portfolio management experience that handles both public markets and alternatives seamlessly.

🖥️ Build the Screen: “Unified Allocation View”
🔹 Layout Requirements

The page should have three main sections:

1️⃣ Consolidated Portfolio Allocation (Donut / Treemap)
Provide a donut chart with the following mock allocation:

Public Equities – 45%

Fixed Income – 30%

Private Equity – 15%

Private Credit – 10% ← must be clickable

When user clicks / hovers on a segment:

Highlight it.

Update the panels on the right automatically.

Add animation: smooth expansion of selected slice.

2️⃣ Private Credit Deep Dive Panel

When Private Credit is selected:

Show these key metrics:

Metrics to Display

NAV Movement (Line Chart) – last 12 months

Capital Breakdown

Drawn: $180M

Undrawn: $70M

Vintage Year Mix (Bar Chart)

2018 – 25%

2019 – 20%

2020 – 30%

2021 – 25%

Contrast With Public Markets

Below the PC metrics, show a small comparison strip:

Public Equities → Realtime price ticks (mock)

Fixed Income → Yields updating every 3 seconds

This demonstrates "alternatives + public markets seamlessly shown together".

3️⃣ Simulation Panel (Interactive Slider)

Add a slider:

Target Private Credit Allocation: [ 5%  —  15% ]


When user moves the slider:

Recalculate a mock portfolio volatility metric:

Example:

At 5% allocation → Volatility: 12.4%

At 10% allocation → Volatility: 11.7%

At 15% allocation → Volatility: 11.1%

Update results instantly, with animations.

Show impact visually:

A small gauge chart or numeric delta:
“Volatility decreased by -0.3%”

CIO-Ready Messaging Section

At the bottom of this page, add a card:

“This prototype demonstrates the platform’s ability to unify public and private assets, handle non-liquid data structures, and run real-time simulations — a structural differentiator in alternative investments infrastructure.”

======================================================
⚙️ USE CASE 2 – Modern Financial Data Platform Migration
======================================================
🎯 Goal

Demonstrate speed, data quality, and operational efficiency improvements after migrating to the new platform.

🖥️ Build the Screen: “Before & After Data Experience”
Layout Requirements

Two vertical side-by-side panels:

1️⃣ Legacy View (Left Side)

Style:

Muted gray palette

Minimal styling

Flat tables

Slightly “slow” appearance

Show:

Legacy Trade Reconciliation Report

Columns: Trade ID, Asset, Quantity, Status, Settlement Date

Data should show:

T+2 delays

Missing settlement confirmations

Some mismatches

Legacy Loading Animation

Add a “5-second simulated load”

Show a slow progress bar

Use setTimeout or equivalent to animate.

At top:

Data Status: Delayed (T+2)

2️⃣ Modern View (Right Side)

Use modern UI:

Colors

Real-time badge

Sharp typography

Smooth micro-interactions

Show:

Real-Time Data Report

Same data set BUT cleansed & current.

Heatmap of Exceptions

Red = settlement issues

Yellow = pending

Green = clean

Instant Search + Filter

Add a search box that instantly filters the table.

Instant Loading Animation

Load time < 1 sec

Show a quick flash animation to emphasize speed.

Add a green indicator:

Data Status: Live
Latency: < 500ms

🏎️ Speed Test Animation (Center or Top)

Create a small component comparing load times:

Legacy → 5.0 seconds [████▒▒▒▒▒▒]
Modern → 0.8 seconds [██████████]


Animate both bars.

CIO-Level Messaging Card

At the bottom:

“The modernization eliminates data latency, improves decision accuracy, and unifies operational workflows. This is foundational for real-time risk, compliance automation, and straight-through processing.”

================================
📡 BACKEND MOCK DATA
================================

Expose the following endpoints:

/api/portfolio-allocation

Returns asset class weights.

/api/private-credit/metrics

Returns NAV movement, vintage mix, drawn/undrawn.

/api/public-market/ticks

Returns random mock ticks refreshed every 3 seconds.

/api/legacy-report

Returns delayed/messy reconciliation data.

/api/modern-report

Returns clean, real-time version of the same data.

================================
📦 FINAL EXPECTATION
================================
The Replit agent should deliver:

A fully functional, clickable web UI

Clean charts

Smooth animations

Mock backend

Clear CIO-grade styling

A demo that clearly shows:

Unified public/private markets capability

Structural understanding of alternatives

Data platform modernization benefits