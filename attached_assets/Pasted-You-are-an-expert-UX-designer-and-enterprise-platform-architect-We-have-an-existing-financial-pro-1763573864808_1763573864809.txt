You are an expert UX designer and enterprise platform architect. 

We have an existing financial prototype application with the following screens:

1. Unified Portfolio Dashboard (donut chart, treemap, consolidated holdings)
2. Private Credit Deep Dive (NAV, Drawn/Undrawn Capital, Vintage Breakdown)
3. Allocation Simulator (slider adjusting Private Credit % and showing portfolio risk impact)
4. Public Markets Data Panel (real-time equities and bond tickers)

We now want to **add an Enterprise-Grade Migration Module** to demonstrate data modernization. The module includes:

1. Migration Project Workspace (all migration programs)
2. Migration Designer (source/target configuration, domain selection, mapping, rules)
3. Migration Execution Simulator (progress bars, task logs, validation status)
4. Data Validation & Reconciliation Console (row counts, nulls, outliers, confidence scores)
5. Lineage & Governance Surface (end-to-end lineage, audit logs, schema drift alerts)
6. Migration Summary Report (KPIs, latency improvement, data quality uplift)

---

### **Instructions:**

1. **Structure the top-level menu/navigation** to logically include both Portfolio screens and the new Migration module.  
   - Suggest tab names, hierarchy, and grouping of screens.
   - Ensure navigation is intuitive for CIO/CTO and Head of Investment Operations.
   - Consider optional collapsible menus for advanced users (e.g., lineage, reconciliation).

2. **Define logical screen flow** for each module:
   - Portfolio/Private Markets module: Dashboard → Private Credit Deep Dive → Allocation Simulator → Public Markets Data.
   - Migration module: Project Workspace → Designer → Execution → Validation → Summary → Lineage.
   - Show where screens can link across modules (e.g., post-migration datasets automatically feed into Portfolio Dashboard).

3. **Add visual hierarchy and contextual labels** to guide the user:
   - Each module should have a consistent header, breadcrumbs, or top navigation indicator.
   - Clearly indicate interactive panels, drilldowns, or simulation screens.

4. **Integration points**:
   - Specify where migration outcomes update portfolio dashboards (e.g., Holdings, NAV, Private Credit panels).
   - Suggest status indicators or small panels in portfolio screens showing “Data Health / Migration Status” per domain.

5. **Output format**:
   - Produce a structured menu outline with all screens, modules, and sub-screens.
   - Include suggested navigation labels, icons, and logical ordering.
   - Provide a flowchart-style explanation (Mermaid or textual hierarchy) showing the end-to-end path a user would take.

6. **Extras** (optional but recommended):
   - Indicate which screens are “hero/demo screens” for CIO storytelling.
   - Suggest shortcuts or quick links for advanced users to drill directly to key migration or validation screens.

---

**Goal:**  
The final structure should present a unified enterprise-grade platform where investment insights (Portfolio + Private Markets) and data modernization (Migration Module) coexist, are navigable, and allow the CIO or Head of Investment Operations to **experience both the portfolio intelligence and the data modernization workflow seamlessly**.
